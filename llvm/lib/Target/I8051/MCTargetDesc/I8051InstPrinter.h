//===- I8051InstPrinter.h - Convert I8051 MCInst to assembly syntax -*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
//
// This class prints an I8051 MCInst to a .s file.
//
//===----------------------------------------------------------------------===//

#ifndef LLVM_I8051_INST_PRINTER_H
#define LLVM_I8051_INST_PRINTER_H

#include "llvm/MC/MCInstPrinter.h"

#include "MCTargetDesc/I8051MCTargetDesc.h"

namespace llvm {

/// Prints I8051 instructions to a textual stream.
class I8051InstPrinter : public MCInstPrinter {
public:
  I8051InstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
                 const MCRegisterInfo &MRI)
      : MCInstPrinter(MAI, MII, MRI) {}

  static const char *getPrettyRegisterName(MCRegister Reg,
                                           MCRegisterInfo const &MRI);

  void printInst(const MCInst *MI, uint64_t Address, StringRef Annot,
                 const MCSubtargetInfo &STI, raw_ostream &O) override;

private:
  static const char *getRegisterName(MCRegister Reg, unsigned AltIdx = I8051::NoRegAltName);

  // Autogenerated by TableGen.
  std::pair<const char *, uint64_t> getMnemonic(const MCInst *MI) override;
  void printInstruction(const MCInst *MI, uint64_t Address, raw_ostream &O);
  bool printAliasInstr(const MCInst *MI, uint64_t Address, raw_ostream &O);
  void printCustomAliasOperand(const MCInst *MI, uint64_t Address,
                               unsigned OpIdx, unsigned PrintMethodIdx,
                               raw_ostream &O);
};

} // end namespace llvm

#endif // LLVM_I8051_INST_PRINTER_H
